<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml">
  <head>
    <link rel="stylesheet" href="/style.css" type="text/css"/>
    <title><%= page['title'] %> - Technomancy</title>
    <meta http-equiv="content-type" content="text/html;charset=UTF-8" />
    <link rel="alternate" href="/feed/atom.xml" title="Technomancy Feed" type="application/atom+xml"/>
    <% if File.exist? "posts/#{page['id'].to_i + 1}.yml" %><link rel="next" href='/<%= page['id'].to_i + 1 %>' /><% end %>
    <% if File.exist? "posts/#{page['id'].to_i - 1}.yml" %><link rel="prev" href='/<%= page['id'].to_i - 1 %>' /><% end %>
  </head>

  <body>
    <h1><a href='/list'><img src='/i/technomancy.png' alt='Technomancy' /></a>
    <a href=''><%= page['title'] %></a></h1>

    <div id='wrapper'>
      <div id='entry'><%= page['content'] %>
      <p class='timestamp'><a href=''><%= timestamp page['timestamp'] %></a></p>
      </div>

      <% comments = parse("comments/#{page['id']}.yml").sort_by {|c| timestamp(c['timestamp']) } %>
      <% unless comments.empty? %>
      <dl id='comments'>
        <% comments.each do |comment| %>
        <dt><%= comment['uri'].to_s == '' ? comment['author'] : "<a href='#{comment['uri']}'>#{comment['author']}</a>" %> 
        &ndash; <span class='timestamp'><%= timestamp comment['timestamp'] %></span></dt>
        <dd><%= comment['content'] %></dd>
        <% end %>
      </dl>
      <% end %>
    </div>

    <div id='footer'>
      <p id='copyright'><a href='/list'>Technomancy</a> is &copy; 2004 &ndash; <%= Time.now.year %> Phil Hagelberg
      <acronym title='Soli Deo Gloria'>SDG</acronym></p>
    </div>
  </body>
</html>