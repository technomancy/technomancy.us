<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="content-type" content="text/html;charset=UTF-8" />
    <link rel="stylesheet" href="/style.css" type="text/css"/>
    <title><%= page['title'] %> - Technomancy</title>
    <script src="/jquery.js" type="text/javascript"></script>
    <script type="text/javascript">
      /* $(document).ready(function () { $('#human').attr('checked', true); }); */
    </script>

    <link rel="alternate" href="/feed/atom.xml" title="Technomancy Feed" 
          type="application/atom+xml"/>
    <link rel="index" href='/list' />
    <% if File.exist? "posts/#{page['id'].to_i + 1}.yml" %><link rel="next" href='/<%= page['id'].to_i + 1 %>' /><% end %>
    <% if File.exist? "posts/#{page['id'].to_i - 1}.yml" %><link rel="prev" href='/<%= page['id'].to_i - 1 %>' /><% end %>
    <% $nodeploy = true if page['draft'] %>
  </head>

  <body>
    <div id="container">
      <div id="logo">
        <a href='/list'><img src='/i/technomancy.png' alt='Technomancy' /></a>
      </div>
      <h1><a href='/<%= page['id'] %>'><%= page['title'] %></a></h1>

      <div id='entry'><%= page['content'] %>
        <p class='timestamp'>
          <% if File.exist? "posts/#{page['id'].to_i - 1}.yml" %><a href='/<%= page['id'].to_i - 1 %>'>&laquo; older</a> |<% end %>
          <a href='/<%= page['id'] %>'><%= Time.stamp page['timestamp'] %></a>
          <% if File.exist? "posts/#{page['id'].to_i + 1}.yml" %>| <a href='/<%= page['id'].to_i + 1 %>'>newer &raquo;</a><% end %></p>
      </div>

      <div id='comments'>
        <dl>
          <% parse("comments/#{page['id']}.yml").sort_by {|c| Time.stamp(c['timestamp']) }.each do |comment| %>
          <dt><%= comment['uri'].to_s == '' ? comment['author'] : "<a href='#{comment['uri']}' rel='nofollow'>#{comment['author']}</a>" %> 
            &ndash; <span class='timestamp'><%= Time.stamp comment['timestamp'] %></span></dt>
          <dd><%= comment['content'].gsub(/[\r\n]/, "<br />") %></dd>
          <% end %>
        </dl>
        <% if page['id'] > 150 %>
        <form action='/comment.cgi' method='post'>
          <p><input type='text' name='author' /> Name</p>
          <p><input type='text' name='uri' /> URL</p>
        
          <input type='hidden' name='post_id' value='<%= page['id'] %>' />

          <textarea id="comment-entry" name='content'></textarea>
          <p><input type='submit' value='Post' />
            Are spammers evil? (yes or no)
            <input type='text' name='evil' id='evil' size="6" />
          </p>
          <p>Comments are manually moderated, sorry. Please no HTML.</p>
        </form>
        <% else %>
        <p>Comments are disabled on older posts.</p>
        <% end %>
      </div>
<%= including "footer" %>
