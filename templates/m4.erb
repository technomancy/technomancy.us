<!DOCTYPE html> <!-*- html -*-->
define(__timestamp, <%= page['timestamp'] %>)dnl
define(__title, `<%= page['title'] %>')dnl
define(__id, <%= page['id'] %>)dnl
include(header.html)
<%= page['content'] %>
include(footer.html)
